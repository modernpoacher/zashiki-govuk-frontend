{% from "govuk/components/fieldset/macro.njk" import govukFieldset %}
{% call govukFieldset(params.params) %}
  {{ caller() if caller }} {#- if statement allows usage of `call` to be optional -#}
{% endcall %}
